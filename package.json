{
  "name": "task-management-system",
  "version": "1.0.0",
  "description": "MERN-based task management system",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
